{% extends 'Layouts/layout_niv2.html.twig' %}

{% block title %}{{ parent() }} : produits{% endblock %}

{% block titre_principal %}{{ parent() }} : détail du produit {{ product.id }}{% endblock %}

{% block vue %}
    {# normalement devrait être dans un fichier css #}
{#    <h2>Détail du produit</h2>#}
{#    #}{# le produit existe : le cas d'erreur a été traité dans le contrôleur #}
{#    <ul>#}
{#        {% for produit in produits %}#}
{#        <li>id : {{ produit.id }}</li>#}
{#        <li>dénomination : {{ produit.libelle }}</li>#}
{#        <li>prix : {{ produit.price }}</li>#}
{#        <li>quantité : {{ produit.quantStock}}</li>#}
{#        {% endfor %}#}
{#    </ul>#}
{#    #}

    <h1> Voici la liste de nos produits </h1>


    <div class="container">
        <div class="row">
            {% for produit in produits  %}
            <div class="col-6 col-lg-3" style="padding: 50px">
                <div class="card" style="width: 18rem;">
                    <img src='https://place-hold.it/286x180/DD725C.gif?text={{ produit.libelle }}' class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">{{ produit.libelle }}</h5>
                        <p class="card-text">{{ produit.price }} €</p>
                        <div>
                            <a href="#" class="btn btn-primary">Ajouter</a>

{#                        //if produit.stockQuantite == 0#}
{#                            //message " produit indisponible"#}
{#                        //else#}
                            <select name="quantChoice" size="2">
                                {% for q in 0..produit.quantStock %}
                                    <option value="{{ q }}">{{ q }}</option>
                                {% endfor %}
                            </select>
                        </div>
{#                            <a href="{{ path('add_panier' , { produit.id , qtt}) }}"></a>#}
{#                        //endif#}
                    </div>
                </div>
            </div>
            {% endfor %}
    </div>


{% endblock %}